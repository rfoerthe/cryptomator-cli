<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.cryptomator.cli</groupId>
  <artifactId>fixture-gen</artifactId>
  <version>0.1.0</version>
  <properties>
    <!-- cryptofs 2.10.0 is built with `maven.compiler.release` 25 (class file version 69), so
         anything below 25 cannot even read its class files: "bad class file ... has wrong version
         69.0, should be 65.0". The legacy generators next door still target 21; their cryptofs
         releases are old enough not to care. -->
    <maven.compiler.release>25</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <exec.mainClass>org.cryptomator.cli.fixtures.Gen</exec.mainClass>
    <!-- Command line passed to Gen: `gen <outputDir>` by default, `verify <vaultDir> <passphrase>`
         with -Dfixture.cmd=verify -Dfixture.arg1=... -Dfixture.arg2=... (empty arguments are dropped). -->
    <fixture.cmd>gen</fixture.cmd>
    <fixture.arg1>${project.basedir}/../../tests/fixtures</fixture.arg1>
    <fixture.arg2></fixture.arg2>
  </properties>
  <dependencies>
    <dependency>
      <groupId>org.cryptomator</groupId>
      <artifactId>cryptofs</artifactId>
      <version>2.10.0</version>
    </dependency>
    <dependency>
      <groupId>com.google.code.gson</groupId>
      <artifactId>gson</artifactId>
      <version>2.13.2</version>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-simple</artifactId>
      <version>2.0.17</version>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.5.0</version>
        <configuration>
          <!-- exec:exec (forked JVM) instead of exec:java: java.nio.file.spi.FileSystemProvider
               discovers the cryptofs provider only through the system class loader, which the
               in-process exec:java class loader is not. -->
          <executable>java</executable>
          <arguments>
            <argument>-classpath</argument>
            <classpath/>
            <argument>${exec.mainClass}</argument>
            <argument>${fixture.cmd}</argument>
            <argument>${fixture.arg1}</argument>
            <argument>${fixture.arg2}</argument>
          </arguments>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
